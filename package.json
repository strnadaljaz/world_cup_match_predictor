{
  "scripts": {
    "test:backend": "cd backend && source venv/bin/activate && python -m pytest",
    "test:frontend": "cd web-interface && npm test",
    "test": "npm run test:backend && npm run test:frontend"
  }
}
